{{template "header" .}}

    <div class="container text-center">
        <div class="row">
            <div class="col">
                
                {{if .IsAuthenticated}}
                    <div>HOME</div>
                {{else}}
                    <div>Debe autenticarse</div>
                {{end}}
                
            </div>
        </div>
    </div>

{{template "footer"}}